/**
 * API client for the Video Processing Pipeline backend.
 *
 * This module is a placeholder populated in ISSUE-20 through ISSUE-22.
 * It will export typed fetch wrappers for every `/api/videos` endpoint,
 * keeping all backend communication in one place.
 *
 * @example
 * ```ts
 * // Future usage (ISSUE-20):
 * import { listVideos, uploadVideo } from "@/api";
 * const videos = await listVideos();
 * ```
 */

export {};
